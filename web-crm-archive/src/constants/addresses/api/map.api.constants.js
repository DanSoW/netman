export const map_default_api = "/map";

const MapApiConstants = {
    map_marks_free: `${map_default_api}/marks/free`,
    map_geocoder_address: `${map_default_api}/geocoder/address`,
}

export default MapApiConstants;