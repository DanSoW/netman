export const media_default_api = "/media";

const MediaApiConstants = {
    get_stats_instructions: `${media_default_api}/download/stats/instructions`,
    instructions_download: `${media_default_api}/download/`,
}

export default MediaApiConstants;